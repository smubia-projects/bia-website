# Intake — raw materials + the living spec

Git-ignored staging area. Nothing here ships. This is where we flesh out **what the
site should become** before any code gets written.

## Working agreement (important — read every session)

- **My role right now is spec, not build.** I help fully flesh out the spec — site map,
  sections, copy direction, links, images, programme models. I do NOT implement until
  the spec is solid and you say go.
- **Keep the theme & design language.** Palette, fonts, motion, the "light base / dark
  pine anchors" system stay. See `DESIGN.md`.
- **Everything else is open.** Number of pages, number of sections per page, number of
  links, all copy — none of it is locked to the current (vibecoded reskin) site. We
  update based on real information as it arrives.
- **Design direction may still change.** Don't treat the current structure as a
  constraint; treat it as a starting sketch.
- **AI Lodge: on hold.** It has a *different operating model* from DAP, so we do not
  design its page until we understand that model. Sonya is sending content. It gets its
  own tab. It ends with a "want your project showcased here? join AI Lodge" CTA.

## Folder layout

- **`SPEC.md`** — the living spec we build together. The real deliverable of this phase.
- **`raw/`** — unprocessed drops: Sonya's docs, your feedback notes, link lists, member
  numbers, anything. Dump it messy; I'll refine it into `SPEC.md`.
- **`images/`** — new image assets, in sub-folders by page/section. Descriptive filenames.

## Priorities / status (edit freely)

- [ ] Links — gather the real URLs (fast, high-impact once we know the final set)
- [ ] Site map — confirm which pages/sections stay, merge, or go
- [ ] Copy direction — page by page, once structure settles
- [ ] AI Lodge — HELD pending operating model + Sonya's content
- [ ] Image swaps
